/**
 * Fixed-position fractal-noise overlay (the grain in the mockup's atmosphere).
 * Purely decorative; hidden from assistive tech.
 */
export function NoiseOverlay() {
  return <div className="noise" aria-hidden="true" />;
}
