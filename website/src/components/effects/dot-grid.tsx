/**
 * Fixed dot-grid background, masked to fade out below the hero — the structural
 * layer of the mockup's atmosphere. Purely decorative.
 */
export function DotGrid() {
  return <div className="grid-bg" aria-hidden="true" />;
}
